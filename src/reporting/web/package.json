{
  "name": "reporting-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Vite + React front-end for the USAT Reporting app (served by server_reporting_8021.js). Kept minimal — only browser libs live here; all server-side deps are in the repo-root package.json.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "deck.gl": "^8.9.35",
    "plotly.js-dist-min": "^2.35.2",
    "programs": "file:../../..",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
