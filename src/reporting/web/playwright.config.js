// Moved: the reporting UI/UX (Playwright) suite lives at src/reporting/e2e (mirrors the merge tool).
// Run it from the repo root with:  npm run reporting_e2e
// This placeholder exists only so a stray `playwright test` in web/ doesn't pick up an accidental config.
export default { testDir: './__no_e2e_here__' };
